document.addEventListener('DOMContentLoaded',()=>{const cursors=document.querySelectorAll('.cursor');setInterval(()=>cursors.forEach(el=>el.style.opacity=el.style.opacity==='0'?'1':'0'),550);const canvas=document.getElementById('matrix-bg');if(!canvas)return;const ctx=canvas.getContext('2d');const chars='アァカサタナハマヤャラワガザダバパABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';let columns=0,drops=[];const resize=()=>{const dpr=Math.min(window.devicePixelRatio||1,2);canvas.width=window.innerWidth*dpr;canvas.height=window.innerHeight*dpr;canvas.style.width=window.innerWidth+'px';canvas.style.height=window.innerHeight+'px';ctx.setTransform(dpr,0,0,dpr,0,0);columns=Math.floor(window.innerWidth/18);drops=Array.from({length:columns},()=>Math.random()*-40)};const draw=()=>{ctx.fillStyle='rgba(5,7,6,.085)';ctx.fillRect(0,0,window.innerWidth,window.innerHeight);ctx.font='14px JetBrains Mono, monospace';for(let i=0;i<columns;i++){const x=i*18;const y=drops[i]*18;ctx.fillStyle='rgba(101,255,138,.72)';ctx.fillText(chars[Math.floor(Math.random()*chars.length)],x,y);if(y>window.innerHeight&&Math.random()>.975)drops[i]=Math.random()*-20;drops[i]+=.7}requestAnimationFrame(draw)};resize();window.addEventListener('resize',resize);draw();});
